name: Dependabot auto-approve

on:
  pull_request:

permissions:
  pull-requests: write
  contents: write

jobs:
  dependabot:
    name: "Dependabot"
    uses: na4ma4/actions/.github/workflows/dependabot-automerge.yml@main
    with:
      merge-type: 'squash'
